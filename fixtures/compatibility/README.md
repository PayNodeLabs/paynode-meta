# Compatibility Fixtures

Files in this directory are reserved for legacy compatibility paths.

They should not be treated as the canonical PayNode protocol. They exist only to help implementations migrate older payload wrappers and header aliases without silently breaking existing integrations.

Add legacy fixtures here only after documenting the behavior in `meta/SPEC.md` or `meta/MULTI_LANGUAGE_BASELINE.md`.
